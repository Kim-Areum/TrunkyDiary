name: BabyDiaryUIKit
options:
  bundleIdPrefix: com.areum
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "26.4"
settings:
  base:
    INFOPLIST_FILE: BabyDiaryUIKit/Info.plist
    PRODUCT_BUNDLE_IDENTIFIER: com.areum.BabyDiaryUIKit
    GENERATE_INFOPLIST_FILE: false
    CODE_SIGN_STYLE: Automatic
    DEVELOPMENT_TEAM: SU5758PVY2
targets:
  BabyDiaryUIKit:
    type: application
    platform: iOS
    sources:
      - path: BabyDiaryUIKit
        excludes:
          - "**/*.plist"
    settings:
      base:
        INFOPLIST_FILE: BabyDiaryUIKit/Info.plist
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_ALTERNATE_APPICON_NAMES: PinkAppIcon
        ASSETCATALOG_COMPILER_INCLUDE_ALL_APPICON_ASSETS: YES
